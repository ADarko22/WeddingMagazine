# Build outputs — produced by ./build.sh and by CI; not tracked.
out/
*.pdf

# CI Pages staging
_site/

# Typst binaries downloaded in CI
typst.tar.xz
typst-*-apple-darwin/

# macOS cruft
.DS_Store
**/.DS_Store
